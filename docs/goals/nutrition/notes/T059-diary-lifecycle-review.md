# T059 diary lifecycle/export review

Approved Worker A from T058. One additive command table must make initial and multi-entry lifecycle operations exactly replayable by principal/key/digest. All nutrient/provenance snapshots are copied server-side from authorized immutable history. Copy creates new series; move and restore append audited corrections; reassignment authorizes both profiles and atomically creates target consumed history plus source deletion. Private export uses `export_data`, stable ordering, bounded JSON, and private no-store/nosniff attachment headers without credentials, sessions, secrets or other profiles. Irreversible deletion/scrubbing remains Worker B after this evidence passes.

The exact GoalBuddy Judge exceeded the single-wait limit and was interrupted. The PM performed the same read-only approval gate as permitted fallback.
