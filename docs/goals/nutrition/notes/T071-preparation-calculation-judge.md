# T071 preparation calculation Judge

Decision: `repair_required`.

Strict identity validation, server-resolved immutable product records, digest/contribution evidence, exclusions/substitutions, per-100g derivation, exactly-one portion basis, historical intake snapshots, accessible controls and explicit no-retention language pass inspection and focused/full tests.

One unsupported assumption and one missing direct oracle must be repaired: explicit edible/drained evidence applied an arbitrary 0.9 confidence multiplier not backed by a source, and tests did not directly compare otherwise identical calculations with different final weights to prove total nutrient mass is invariant. Preserve the immutable food record's confidence (while retaining estimated labels/evidence notes) and add the direct total-mass/per-100g regression.

The exact GoalBuddy Judge exceeded the single 30-second wait and was interrupted. The PM performed the same read-only gate as permitted fallback.
