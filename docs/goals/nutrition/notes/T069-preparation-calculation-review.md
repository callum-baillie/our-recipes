# T069 preparation calculation review

Decision: `approved` for the first Worker package.

The first package may use existing persistence to add final cooked weight, required/optional exclusions, substitutions resolved to current immutable product records, explicit bounded edible/drained factors with evidence notes, total/per-serving/per-100g summaries, and exactly-one serving-count or weighed-portion recipe consumption. Final weight changes concentration and portion scaling only. Every nutrient/source value remains server-owned and every factor/identity enters the immutable digest and contribution evidence.

Nutrient-specific retention is deferred to a separate sourced module: T070 must not accept or invent retention factors. Prepared-instance alignment/recalculation is also a separate Worker after T070 passes.

The exact GoalBuddy Judge exceeded the single 30-second wait and was interrupted. The PM performed the same read-only approval gate as permitted fallback.
