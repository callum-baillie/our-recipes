# T077 recipe library and planner boundary Judge

Decision: `approved`.

The T076 map identifies the correct pre-pagination query seam and keeps generic factual recipe discovery separate from private personalized recommendations. A correlated latest-current-calculation expression can participate in SQL conditions, total count, ordering, limit and offset without changing schema or producing false pagination. URL-selected compact facts satisfy a bounded user choice while preserving current defaults, neutral units, stale suppression and missing-is-unknown semantics.

The primary planner adapter has a safe authorization seam: resolve the signed Nutrition cookie on the server, enumerate only profiles accessible to that principal, and call the existing authorized projection and diary services. Household profile selection remains a headcount/planning convenience and must be labeled as distinct from explicit Nutrition allocations. No session means no private query or projection—only a neutral unlock link.

Pantry T038 ownership is disjoint. Personalized remaining-target matching is deliberately deferred to the deterministic recommendation slice so goals, diary data, allergies, exclusions, sufficient-data thresholds, permission scope, explanations and dismissals are handled together rather than becoming an opaque recipe score.

The exact GoalBuddy Judge exceeded the single 30-second wait and was interrupted. The PM performed the same read-only gate as permitted fallback.
