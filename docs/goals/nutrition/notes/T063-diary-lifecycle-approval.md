# T063 diary lifecycle/export approval

Decision: `approved`.

T060 plus T062 now satisfy the non-destructive diary lifecycle boundary. Immutable server-owned snapshots, latest-revision semantics, atomic day/reassignment operations, retry digest matching and replay reauthorization, dual-profile authorization, trusted-origin API handling, accessible controls, export permission and private attachment headers are present. Export performs count preflight before materialization, conservatively bounds prepared links, joins nutrient children, chunks prepared IDs, retains a final 32 MiB bound, and reduces foreign command targets to a `redacted` semantic marker without foreign profile/revision IDs, digests, retry keys or principals.

Worker B may now implement irreversible selected-profile deletion only with `delete_data`, exact `DELETE <display name>` confirmation, expected profile version, transaction-local dependency ordering, sensitive-field scrubbing and archival. It must preserve household recipes, calculations, reference systems, Pantry and every other Nutrition profile.

The exact GoalBuddy Judge exceeded the single 30-second wait and was interrupted. The PM performed the same read-only gate as permitted fallback.
