# T046 — Planner locator threshold

T046 reached its two-failure stop threshold before a planning mutation. The semantic recipe and meal selects work; the remaining failure is that `Free-form meal` names both the radio (via `A free-form meal`) and title textbox. A fresh test task must use `input[name="title"]` for the textbox before rerunning browser proof. No product behavior, migration, service, or API failure has been observed.
