{
  "result": "done",
  "changed_files": [
    "src/lib/domain/pantry-availability.ts",
    "src/lib/services/pantry-availability-service.ts",
    "tests/unit/pantry-availability.test.ts",
    "tests/integration/pantry-availability-service.test.ts"
  ],
  "commands": [
    { "cmd": "pnpm exec prettier --check <T013 scoped files>", "status": "pass" },
    { "cmd": "pnpm exec vitest run --project unit tests/unit/pantry-availability.test.ts", "status": "pass", "summary": "7 tests passed" },
    { "cmd": "pnpm exec vitest run --project integration tests/integration/pantry-availability-service.test.ts", "status": "pass", "summary": "4 tests passed" },
    { "cmd": "pnpm test", "status": "pass", "summary": "142 unit and 55 integration tests passed" },
    { "cmd": "pnpm lint", "status": "pass" },
    { "cmd": "pnpm typecheck", "status": "pass" },
    { "cmd": "pnpm openapi:validate", "status": "pass", "summary": "Valid with 11 existing warnings" },
    { "cmd": "git diff --check", "status": "pass" }
  ],
  "summary": "Implemented required-first shared-stock allocation while restoring original response order. Ingredient reads explicitly order by group position, ingredient position, and stable ID. Added optional-first and repeated-read regressions while preserving uncertainty, dates, attribution, recipe text, origin checks, and non-mutation."
}
