# T013 required-first allocation

Required recipe ingredients now receive compatible exact shared stock before optional lines, while the response preserves original ingredient order. Ingredient mapping reads explicitly order by group position, ingredient position, and stable ingredient ID. Repeated service reads return identical explanations.

Verification passed: 7 focused unit, 4 focused integration, 142 full unit, and 55 full integration tests plus formatting, lint, TypeScript, OpenAPI, and diff checks.
