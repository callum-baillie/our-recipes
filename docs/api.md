# API conventions

All routes are under `/api/v1`. Responses use JSON, all write endpoints require an exact same/trusted `Origin`, and failures use:

```json
{ "error": { "code": "machine_readable_code", "message": "Safe human explanation" } }
```

| Endpoint                                                        | Purpose                                                                                                                                       |
| --------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| `GET /api/v1/health`                                            | Liveness/readiness with a non-sensitive `setupComplete` flag.                                                                                 |
| `GET /api/v1/ai/status`                                         | Reports only the non-secret configured/unconfigured AI connection state and supported actions.                                                |
| `POST /api/v1/ai/reviews`                                       | Explicitly requests a bounded, review-only OpenAI suggestion from pasted text or existing normalized import scans.                            |
| `POST /api/v1/setup`                                            | Creates the one household plus its first profile; sets the signed active-profile cookie.                                                      |
| `GET /api/v1/profiles`                                          | Lists local household profiles.                                                                                                               |
| `POST /api/v1/profiles`                                         | Adds a profile after setup.                                                                                                                   |
| `PATCH /api/v1/profiles/{profileId}`                            | Updates a profile’s display and cooking preferences.                                                                                          |
| `POST /api/v1/profiles/{profileId}`                             | Archives or restores a profile; the active and last usable profile are protected.                                                             |
| `PATCH /api/v1/profiles/{profileId}/active`                     | Selects a known profile and rotates the signed selection cookie.                                                                              |
| `GET`/`POST /api/v1/tags`                                       | Lists household tag metadata/usage or creates a named colored tag.                                                                            |
| `PATCH`/`DELETE /api/v1/tags/{tagName}`                         | Renames/colors a tag or removes it from every recipe.                                                                                         |
| `POST /api/v1/tags/{tagName}/merge`                             | Reassigns every recipe using a tag to a target tag, then removes the source tag.                                                              |
| `GET`/`POST /api/v1/collections`                                | Lists manually ordered collections or creates a curated collection.                                                                           |
| `POST /api/v1/collections/order`                                | Replaces the complete manual collection order after stale-list validation.                                                                    |
| `GET`/`PATCH`/`DELETE /api/v1/collections/{collectionId}`       | Reads, edits, or removes one collection; deletion never removes recipes or photos.                                                            |
| `PUT /api/v1/collections/{collectionId}/recipes`                | Replaces the ordered many-to-many recipe membership of one collection.                                                                        |
| `GET /api/v1/recipes`                                           | Searches a paginated shared library with bounded query, creator, tag, metadata, time, favorite/history, lifecycle, and sort facets.           |
| `POST /api/v1/recipes`                                          | Creates a structured shared recipe and first actor-attributed revision.                                                                       |
| `GET /api/v1/recipes/{recipeId}`                                | Reads the current structured recipe card and revision metadata.                                                                               |
| `GET /api/v1/recipes/{recipeId}/export`                         | Downloads a deterministic `application/ld+json` Schema.org Recipe document without household-private fields.                                  |
| `GET /api/v1/recipes/{recipeId}/export/markdown`                | Downloads a deterministic Markdown card with shared recipe metadata only.                                                                     |
| `GET /api/v1/exports/recipes`                                   | Streams one server-generated portable `.tar.gz` containing deterministic per-recipe JSON-LD, referenced normalized WebP files, and checksums. |
| `PUT /api/v1/recipes/{recipeId}/preference`                     | Saves the active profile’s private 1–5 rating and kitchen note without changing the shared revision.                                          |
| `POST /api/v1/recipes/{recipeId}/revisions/{revision}/restore`  | Replays one validated saved shared snapshot as a new conflict-protected recipe revision.                                                      |
| `PUT /api/v1/recipes/{recipeId}`                                | Replaces the current recipe graph and appends an actor-attributed revision when `expectedRevision` matches.                                   |
| `POST /api/v1/recipes/{recipeId}/duplicate`                     | Creates an active editable copy of a current recipe under the active profile.                                                                 |
| `PATCH /api/v1/recipes/{recipeId}/status`                       | Moves a recipe between active, archived, and trash lifecycle states with revision protection.                                                 |
| `POST /api/v1/recipes/{recipeId}/images`                        | Adds one local JPEG/PNG/WebP recipe photo after server-side normalization.                                                                    |
| `POST /api/v1/recipes/{recipeId}/images/generate`               | Explicitly generates one serving image through OpenAI, then validates and stores it locally.                                                  |
| `GET`/`DELETE /api/v1/recipes/{recipeId}/images/{imageId}`      | Reads a normalized WebP recipe photo or removes its local media and metadata.                                                                 |
| `GET`/`POST /api/v1/meal-plan`                                  | Reads an inclusive date range or adds a recipe-linked/free-form breakfast, lunch, dinner, or snack meal.                                      |
| `DELETE /api/v1/meal-plan/{entryId}`                            | Removes a planned meal.                                                                                                                       |
| `POST /api/v1/meal-plan/duplicate`                              | Copies valid source-week entries to a destination week as independent rows.                                                                   |
| `GET /api/v1/meal-plan/export`                                  | Downloads a deterministic local ICS calendar for an inclusive date range.                                                                     |
| `GET`/`POST /api/v1/shopping-lists`                             | Lists generated lists or creates a new editable list from recipe-linked plan rows in a week.                                                  |
| `GET`/`PATCH /api/v1/shopping-lists/{listId}`                   | Reads a list with household aisle definitions or saves its explicit item order.                                                               |
| `POST /api/v1/shopping-lists/{listId}/items`                    | Adds a manual bounded list item, optionally assigned to a household aisle.                                                                    |
| `PATCH`/`DELETE /api/v1/shopping-lists/{listId}/items/{itemId}` | Edits/checks/reassigns or removes one list item.                                                                                              |
| `GET`/`POST`/`PATCH /api/v1/shopping-lists/aisles`              | Lists, creates, or replaces the complete household aisle order.                                                                               |
| `DELETE /api/v1/shopping-lists/aisles/{aisleId}`                | Removes an aisle and leaves affected list rows explicitly unassigned.                                                                         |

| `PUT /api/v1/recipes/{recipeId}/favorite` | Sets the current profile’s personal favorite state. |
| `POST /api/v1/cook-sessions` | Starts a per-profile cook session with a target serving count. |
| `POST /api/v1/cook-sessions/{sessionId}/complete` | Completes the current profile’s cook session. |
| `POST /api/v1/capture-drafts` | Creates a non-persistent review draft from pasted text, or lists/selects deterministic Recipe candidates from a hardened public URL. |
| `POST /api/v1/jsonld-drafts` | Finds locally parsed Schema.org Recipe candidates from bounded pasted JSON-LD, or returns the explicitly selected editable draft. |
| `POST /api/v1/jsonld-drafts/confirm` | Validates an edited JSON-LD review draft and creates a recipe only after explicit confirmation. |
| `POST /api/v1/imports` | Creates a local one-PDF or ordered one-to-four scan import operation and review draft from multipart files plus optional combined transcription. |
| `GET /api/v1/imports/{importId}` | Reads the local import provenance and reproducible review draft. |
| `GET /api/v1/imports/{importId}/file` | Streams a normalized scan privately or downloads the accepted PDF. |
| `GET /api/v1/imports/{importId}/files/{artifactId}` | Streams one import-scoped normalized scan artifact privately. |
| `POST /api/v1/imports/{importId}/confirm` | Validates the edited review draft, creates its recipe, and records one immutable confirmation link. |
| `GET`/`POST /api/v1/backups` | Lists local recovery bundles or creates a consistent manual database/media backup. |
| `GET /api/v1/backups/{backupId}` | Validates a listed local bundle in isolated staging and returns its restore preview. |
| `GET /api/v1/backups/{backupId}/download` | Streams a listed local backup as a download for offline safekeeping. |
| `POST /api/v1/backups/{backupId}/restore` | Requires exact `RESTORE` confirmation, creates a safety backup, then restores a validated local bundle. |

The API is intentionally not an authentication boundary. Do not expose it to the public internet without adding real authentication and a revised threat model.

The AI status endpoint has no enable/configure action and never returns a key. Production reads `OPENAI_API_KEY` only from the server environment; local development may use the ignored root `.api_keys` convenience file. `POST /ai/reviews` requires `confirm: true` plus either 20–30,000 characters of text or an existing image-import ID. `POST /recipes/{recipeId}/images/generate` requires `confirm: true`. Both require a trusted origin and active profile, enforce a four-starts-per-profile ten-minute process limit, return an editable suggestion or local image ID, and write a no-raw-content audit. No endpoint auto-saves an AI review candidate or returns provider URLs/secrets.

Recipe reads and writes include ordered ingredient groups, instruction sections, equipment, original-author/source/cooking-method metadata, optional user-entered nutrition values, current creator/editor attribution, and revision metadata. The editor sends `expectedRevision` on every shared replacement, lifecycle change, or restore; a stale value returns `409 recipe_revision_conflict` rather than overwriting a newer household edit. Restoring a saved revision validates its stored bounded recipe shape and appends a new revision—it never mutates the old snapshot. Profile ratings/notes are separate and do not increment that revision. Recipe-list reads default to active cards and 24 cards per page. Their bounded query parameters are `q`, `creator`, `tag`, `collection`, `category`, `cuisine`, `maxTotalMinutes`, `favorite=true`, `cooked=true`, `status`, `sort`, and `page`; `sort=highest-rated` uses only the selected profile’s rating and falls back to recency without one.

Tags are household-managed, lower-cased labels. Rename, merge, and deletion update recipe associations in a database transaction and refresh full-text search; deletion removes the tag association rather than a recipe. Profiles remain attribution/preferences only and are never an authorization boundary.

The portable full-recipe export is a download-only, no-store `application/gzip` response. It requires a signed active-profile selection and rejects an explicit untrusted `Origin`; a normal local browser download has no `Origin` header. The service creates a private temporary staging directory, writes only generated JSON-LD plus regular normalized WebP copies, records a format/version/snapshot timestamp, stable archive paths, byte lengths, and SHA-256 checksums in `manifest.json`, then streams the resulting tarball and deletes the staging/archive files. It accepts no archive bytes, files, paths, or restore request. Profile names/IDs, ratings/notes, revisions, import/OCR records, source storage keys, and configuration are excluded. The export covers all current recipe records; lifecycle and other application-only state remain recoverable through the separate local backup format.

Collections are household-managed named shelves. Recipe membership is ordered and many-to-many; replacing a shelf’s membership never changes the recipe graph or its revisions. A cover may refer only to an existing normalized photo of a recipe already in that collection. Removing a collection removes only its associations, and removing a covered recipe/photo clears the cover rather than retaining a broken reference.

Recipe-photo upload uses `multipart/form-data` fields named `image` and optional `altText`. It ignores the client MIME type and filename, rejects non-JPEG/PNG/WebP signatures, and returns `413` for photos above 10 MB. The response never exposes the durable storage key.

Document import accepts repeated `files` multipart fields (with legacy single `file` compatibility), optional `transcription`, and an optional `autoOpenAiVision=true` UI hint. It permits exactly one PDF or one to four JPEG/PNG/WebP scans, derives every accepted type from bytes, and caps all submitted source bytes at 15 MB. Browser HEIC/HEIF conversion replaces original files with JPEG before this request; optional bounded `clientConversions` metadata explains that provenance, but raw HEIC/HEIF remains rejected by the server. PDFs remain limited to 12 pages/100,000 extracted characters. Each scan is metadata-stripped and stored as its own opaque normalized WebP artifact with source hash/name/order. For an image scan with no transcription and this hint, the endpoint creates a local `openai-vision-pending` review operation; the UI then makes the separately trusted-origin, credential-gated `vision-extraction` request. No provider call occurs from file selection or the import endpoint itself. The candidate remains editable and confirmation is still the only recipe write. Without the hint, clear English scan sets may use the pinned local Tesseract model; blank, low-confidence, timeout, or unavailable local OCR returns `422 text_required`, while textless PDFs always need transcription. The route is rate-limited to six starts per active profile per ten-minute process window; `422 rate_limited` asks the caller to wait. A confirmed operation returns `409` on replay.

JSON-LD portability uses JSON request field `source` (paste text, maximum 1 MB) and optional `candidateIndex`. The first request returns only bounded Schema.org `Recipe` candidates from a root object, array, or `@graph`; a second request with the selected index returns an editable draft. No request follows a URL, reads a file, accepts an archive, or stores the pasted source. `POST /jsonld-drafts/confirm` accepts the ordinary bounded recipe shape after review. Export returns `application/ld+json` with the portable title, summary, yield, ISO 8601 prep/cook/total durations, category/cuisine/tags, source URL, structured ingredients, and structured steps; it omits actor data, household notes, tips, local media, and internal storage information.

Markdown export is a local, deterministic text rendering of the current shared recipe card, including optional author/source, cooking method, equipment, and nutrition values exactly as entered. It deliberately omits profile-specific ratings and notes. Nutrition is a bounded user input field only; neither route nor service performs a lookup, estimate, or provider call.

For `kind: "url"`, `POST /capture-drafts` keeps the existing safe server-side fetch controls and never requests browser-side page access. It returns `candidates` for bounded embedded Schema.org JSON-LD Recipe nodes (including `@graph`/multiple nodes), or Microdata Recipes when JSON-LD is absent. Resubmit the same URL with a displayed `candidateIndex` to receive an editable `draft`. If no structured Recipe candidate exists, an explicitly warned Open Graph/text draft remains review-only. HTML, extracted text, and candidate data are not persisted by this endpoint; image URLs are not downloaded.

Meal-plan POST input requires an ISO local date, a named meal slot, 1–100 servings, a bounded note, and exactly one useful source: a current recipe ID or a bounded free-form title. `POST /meal-plan/duplicate` accepts source/destination week starts and copies only valid entries. `GET /meal-plan/export` requires `start`/`end` ISO dates and returns `text/calendar` with attachment, `no-store`, and `nosniff` headers; it performs no remote calendar integration. Shopping-list generation intentionally skips free-form meals. Its output remains an independent editable list even when a source week later changes.

Shopping aisle names are 1–80 trimmed characters and unique for the household. Reorder requests must name the complete current aisle set, preventing a stale client from silently dropping an aisle. Item writes accept only a current aisle ID or an empty assignment. The list read returns aisles in saved position order; clients should render those groups followed by **Unassigned**. Deleting an aisle never deletes a list item.

Backup restore never accepts an uploaded archive. It operates only on a backup already listed by the server, validates its archive path/type/manifest/checksums/database integrity in isolated staging, and requires an active profile plus a trusted mutation origin for create/restore actions.
